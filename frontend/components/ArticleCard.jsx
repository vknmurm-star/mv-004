import Link from 'next/link'
import Image from 'next/image'
import { getCategory } from '../lib/content'
export default function ArticleCard({ article, featured=false }){const cat=getCategory(article.category);return <article className={`card overflow-hidden ${featured?'md:grid md:grid-cols-[1.15fr_.85fr]':''}`}><Link href={`/articles/${article.slug}`} className="block bg-[var(--soft)]"><Image src={article.coverImage} alt={article.coverImageAlt} width={900} height={520} className="h-full min-h-64 w-full object-cover"/></Link><div className="p-7 md:p-9"><span className="pill">{cat?.name}</span><h2 className="display mt-4 text-2xl leading-snug md:text-3xl"><Link href={`/articles/${article.slug}`}>{article.title}</Link></h2><p className="mt-3 text-[var(--muted)]">{article.description}</p><div className="mt-6 flex items-center justify-between text-sm text-[var(--muted)]"><time dateTime={article.date}>{new Intl.DateTimeFormat('ru-RU',{day:'numeric',month:'long',year:'numeric'}).format(new Date(article.date))}</time><span>{article.readingTime} мин</span></div></div></article>}
