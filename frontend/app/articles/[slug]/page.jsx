import Image from 'next/image'
import { notFound } from 'next/navigation'
import JsonLd from '../../../components/JsonLd'
import { articleJsonLd, breadcrumbs, getAllArticles, getArticle, getCategory, renderMarkdown, siteUrl } from '../../../lib/content'
export function generateStaticParams(){return getAllArticles().map(a=>({slug:a.slug}))}
export async function generateMetadata({params}){const { slug } = await params;const a=getArticle(slug);if(!a)return {};return {title:a.title,description:a.description,alternates:{canonical:`/articles/${a.slug}`},openGraph:{title:a.title,description:a.description,url:`${siteUrl}/articles/${a.slug}`,type:'article',publishedTime:a.date,images:[{url:a.coverImage,width:1200,height:680,alt:a.coverImageAlt}]},twitter:{card:'summary_large_image',title:a.title,description:a.description,images:[a.coverImage]}}}
export default async function ArticlePage({params}){const { slug } = await params;const a=getArticle(slug);if(!a)notFound();const cat=getCategory(a.category);return <main><JsonLd data={articleJsonLd(a)}/><JsonLd data={breadcrumbs([{name:'Главная',href:'/'},{name:cat?.name||'Рубрика',href:`/category/${a.category}`},{name:a.title,href:`/articles/${a.slug}`}])}/><article className="container py-12"><div className="mx-auto max-w-3xl text-center"><span className="pill">{cat?.name}</span><h1 className="display mt-5 text-5xl leading-none md:text-7xl">{a.title}</h1><p className="mt-6 text-xl leading-8 text-[var(--muted)]">{a.description}</p><div className="mt-5 text-sm text-[var(--muted)]"><time dateTime={a.date}>{new Intl.DateTimeFormat('ru-RU',{day:'numeric',month:'long',year:'numeric'}).format(new Date(a.date))}</time> · {a.readingTime} мин чтения</div></div><Image src={a.coverImage} alt={a.coverImageAlt} width={1200} height={680} className="my-10 rounded-[32px] border border-[var(--line)] object-cover"/><div className="prose mx-auto max-w-3xl" dangerouslySetInnerHTML={{ __html: renderMarkdown(a.content, a.slug, a.category) }} /></article></main>}
